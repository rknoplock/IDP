apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  labels:
    app: {{ .Release.Name }}-dashboard-api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-dashboard-api
