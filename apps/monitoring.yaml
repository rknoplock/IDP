apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: kube-prometheus-stack
    targetRevision: "86.2.3"
    helm:
      values: |
        # Sem persistência por enquanto - dados em memória, perdidos em restart
        prometheus:
          prometheusSpec:
            retention: 24h
            storageSpec: {}
            image:
              registry: quay.io
              repository: prometheus/prometheus
              tag: v3.12.0
        alertmanager:
          alertmanagerSpec:
            storage: {}
          # desabilita alertas por enquanto, sem receiver configurado
          config:
            route:
              receiver: 'null'
            receivers:
              - name: 'null'

        grafana:
          persistence:
            enabled: false
          service:
            type: ClusterIP
            port: 3000
          # usuario admin / senha gerada, ver comando abaixo pra recuperar
          adminPassword: admin

        # Docker Desktop nao suporta mount propagation shared/slave pra "/" do host,
        # entao desabilita esse mount especifico do node-exporter (path / is mounted...)
        prometheus-node-exporter:
          hostRootFsMount:
            enabled: false

        # ativa monitoramento automatico dos componentes do proprio cluster
        kubeControllerManager:
          enabled: false
        kubeScheduler:
          enabled: false
        kubeEtcd:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true